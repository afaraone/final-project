sudo: required
dist: trusty

language: ruby
rvm:
  - 2.5.1

before_script:
  - cd lifestyle-app-backend
  - bundle
  - bundle exec rails db:create
  - bundle exec rails db:migrate RAILS_ENV=test

script:
  - bundle exec rspec
  - bundle exec rubocop
